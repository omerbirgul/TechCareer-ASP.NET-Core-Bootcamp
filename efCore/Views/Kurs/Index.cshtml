@model IEnumerable<Kurs>

    <h2>Kurs Ekle</h2>
    <a asp-action="Create" asp-controller="Kurs" class="btn btn-outline-success">Ekle</a>

    <table class="table">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(m => m.KursId)</th>
                <th>@Html.DisplayNameFor(m => m.Baslik)</th>
                <th style="width: 150px;"> Ayarlar</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.KursId)</td>
                    <td>@Html.DisplayFor(modelItem => item.Baslik)</td>
                    <td>
                        <a asp-controller="Kurs" asp-action="Edit" asp-route-id="@item.KursId" class="btn btn-sm btn-primary">Düzenle</a>
                        <a asp-controller="Kurs" asp-action="Delete"asp-route-id ="@item.KursId" class="btn btn-sm btn-danger">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>